# Chen's Engineer Toolbox - Requirements
# Python 3.11+

# Core Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
streamlit>=1.39.0

# Data Processing
pandas>=2.2.3
numpy>=1.26.0
openpyxl>=3.1.5

# API & Network
httpx>=0.27.2
openai>=1.0.0
pydantic>=2.9.2
python-multipart>=0.0.12
python-dotenv>=1.0.1

# Visualization
plotly>=5.24.1
matplotlib>=3.7.0  # Reliable fallback for image export
kaleido==0.2.1  # Use 0.2.1 (stable version with chromium binary included)
# plotly-orca  # Alternative image export engine (optional fallback if kaleido fails)

# Document Generation (Metal Loss Assessment)
python-docx>=1.1.0

# Inspection Report PDF Parser
pdfplumber>=0.11.0
pymupdf>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0
surya-ocr>=0.6.0  # Fast document OCR (~1-2s/page CPU); primary engine in auto mode
transformers>=4.56.1,<5  # surya-ocr: transformers 5.x breaks init (SuryaDecoderConfig.pad_token_id); see surya#484
easyocr>=1.7.0  # Better table OCR; fallback when surya is unavailable

# Excel to PDF Conversion (Dig Package Generator - Windows only)
pywin32>=306; sys_platform == 'win32'

# Development dependencies (optional)
# Install with: pip install -r requirements.txt -r requirements-dev.txt 